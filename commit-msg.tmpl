#!/bin/sh

commit_regex='(%JIRA%-[0-9]+|Merge)'
error_msg="Aborting commit. Your commit message is missing either a JIRA Issue ('%JIRA%-1111') or 'Merge'"

if ! grep -iqE "$commit_regex" "$1"; then
    echo "$error_msg" >&2
    exit 1
fi
